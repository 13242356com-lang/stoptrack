{
  "name": "stoptrack-sync-server",
  "version": "1.0.0",
  "private": true,
  "description": "Reference sync backend for StopTrack (offline-first ASLA downtime tracker). Zero dependencies — pure Node.",
  "main": "server.js",
  "scripts": {
    "start": "node server.js"
  },
  "engines": {
    "node": ">=16"
  },
  "optionalDependencies": {
    "nodemailer": "^6"
  }
}
